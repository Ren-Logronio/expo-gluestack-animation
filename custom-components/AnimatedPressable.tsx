import { AnimatedPressable } from "@gluestack-style/animation-resolver";
import { styled } from "@gluestack-style/react";

const CustomAnimatedPressable = styled(AnimatedPressable, {
    h: "$16", w: "$16", backgroundColor: "$rose500"
})

export default CustomAnimatedPressable;